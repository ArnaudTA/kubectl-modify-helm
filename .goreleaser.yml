builds:
- id: kubectl-modify-release
  main: ./
  binary: kubectl-modify_release
  env:
  - CGO_ENABLED=0
  ldflags:
  - "-s -w -X github.com/ArnaudTA/kubectl-modify-release/pkg/cmd.Version={{.Version}}"
  goos:
  - darwin
  - linux
  - windows
  goarch:
  - amd64
  - arm64

archives:
- builds:
  - kubectl-modify-release
  name_template: "{{ .ProjectName }}_{{ .Tag }}_{{ .Os }}_{{ .Arch }}{{ if .Arm }}v{{ .Arm }}{{ end }}"
  wrap_in_directory: false
  format: tar.gz
  files:
  - LICENSE
